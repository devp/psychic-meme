{
  "// ": "Typecheck only -- noEmit means the .js files ship byte-identical.",
  "//exclude": "sw.js and precache-manifest.js are worker scope (see tools/check-sw.mjs); verify-browser.mjs imports playwright, an optional dev tool.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["lib/**/*.js", "variants/**/*.js", "tools/**/*.mjs"],
  "exclude": [
    "**/vendor/**",
    "**/sw.js",
    "**/precache-manifest.js",
    "tools/check-sw.mjs",
    "tools/verify-browser.mjs",
    "node_modules"
  ]
}
